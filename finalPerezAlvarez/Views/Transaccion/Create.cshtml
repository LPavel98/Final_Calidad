@using Microsoft.AspNetCore.Components.Forms

<div class="container">
    <div class="row">
        <div class="col-6 offset-3">
            <h1>Nueva Transaccion</h1>
            <form action="/transaccion/create?idCuenta=@ViewBag.idCuenta" method="post">
                <div>
                    <label for="tipo">Tipo</label>
                    <select class="form-select" name="Tipo" id="Tipo">
                        <option value="INGRESO">Ingreso</option>
                        <option value="GASTO">Gasto</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label for="fecha">Fecha</label>
                    <input type="datetime-local" class="form-control" Name="fecha" id="fecha" value="@Model.fecha">
                    @Html.ValidationMessage("fecha")
                </div>
                
                <div class="mb-3">
                    <label for="descripcion">Descripcion</label>
                    <input type="text" class="form-control" Name="descripcion" id="descripcion" value="@Model.descripcion">
                    @Html.ValidationMessage("descripcion")
                </div>
                

                <div class="mb-3">
                    <label for="monto">Monto</label>
                    <input type="text" class="form-control" Name="monto" id="monto" value="@Model.monto">
                </div>
                @Html.ValidationMessage("monto", new{id="montoValidacion"})
                @*<div class="mb-3">
                    <label for="Nota">Nota</label>
                    <input type="text" class="form-control" Name="Nota" id="Nota" value="@Model.Nota">
                </div>*@

                <br>
                <button class="btn btn-primary">Guardar</button>
                <a href="/cuenta/index" class="btn btn-link">Cancelar</a>
            </form>
        </div>
    </div>

</div>